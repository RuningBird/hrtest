name: '222'
'on':
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:
    inputs:
      opt:
        description: desm
        required: false
        type: choice
        options:
          - b
        default: b
env:
  d: '21'
jobs:
  job-1:
    name: Job 1
    steps:
      - name: echo hello
        run: echo "Hello"
    timeout-minutes: 30
    runs-on: ubuntu-latest
